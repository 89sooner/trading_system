# Doc sync checklist

- Does the README still match the actual commands?
- Are config examples using the current field names?
- If a package was added or renamed, is the layout documented?
- If behavior changed, is there at least one concrete usage example?
